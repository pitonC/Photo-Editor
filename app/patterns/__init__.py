"""Implementación de los cuatro patrones de diseño del editor.

* :mod:`app.patterns.prototype`  — clonar capas (imagen / forma).
* :mod:`app.patterns.decorator`  — filtros encadenables sobre una imagen.
* :mod:`app.patterns.command`    — undo / redo de cualquier acción.
* MVC / MTV se materializa en `app/__init__.py`, `app/models.py`,
  `app/routes/*.py` y `app/templates/*.html`.
"""
